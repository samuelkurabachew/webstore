server:
  port: 7071

#MongoDb Config
spring:
  application:
    name: product-service
  zipkin:
    base-url: http://localhost:9411/
sleuth:
  sampler:
    probability: 1

  data:
    mongodb:
      host: localhost
      port: 27017
      database: test
